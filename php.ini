; Symfony CLI reads this project-local configuration.
upload_max_filesize = 50M
post_max_size = 260M
; Parse more than the application limit so excess files produce a validation error.
max_file_uploads = 20
